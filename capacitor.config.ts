import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'castro.filminho',
  appName: 'Filminho',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
